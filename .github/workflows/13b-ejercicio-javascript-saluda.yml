name: 13b - Ejercicio javascript saluda
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  uso-compuesto:
    runs-on: ubuntu-latest
    name: Uso acción javascript
    steps:
      - id: compuesta
        uses: juancho85/gha-javascript-hola@v1
        with:
          nombre: 'Juan'
      - name: El saludo
        run: echo "${{steps.compuesta.outputs.saludo}}"