name: 14b - Ejercicio docker dados
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  uso-compuesto:
    runs-on: ubuntu-latest
    name: Uso acción docker python
    steps:
      - id: docker
        uses: juancho85/gha-dockerpy-dice@v1
        with:
          numero-dados: 'dos'
      - name: Dado 1
        run: echo "${{steps.docker.outputs.dado1}}"
      - name: Dado 2
        run: echo "${{steps.docker.outputs.dado2}}"